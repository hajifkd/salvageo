{
  "name": "salvageo",
  "version": "0.1.0",
  "description": "Redirect webpages on terminated hosting services to the wayback machine",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  }
}
